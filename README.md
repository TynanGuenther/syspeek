# syspeek

`syspeek` is a small Linux command-line tool written with c++ that prints basic system information using kernel interfaces

## Features
- Hostname and kernel version
- System uptime
- CPU model
- memory usage
- Disk usage of root filesystem


## Build
```bash
make

